## GitHub Actions

- ./hooks/pre-commit (isort, black, flake8)
- ./hooks/pre-push (bandit, тесты)
- валидация билда (flake8, bandit, тесты) на develop и master

## Terraform

- развернуть всю инфраструктуру

## Ansible (под вопросом)?

- выполнение скриптов (установка конфигов, либ, переменных окружения) на виртуалках

Подумать, либо запускать плейбуки, либо подготовить заранее контейнеры
(учитывая, кто некоторые сервисы будут подняты как managed services, то подгатавливать контейнеры не очень удобно).

## MLFlow

В kubernetes? как будто аналогично airflow

- отслеживание обучения (любые запуски в трекинг)
- хранение моделей для деплоя

## Airflow

Развернуть ли в kubernetes? По идее можно, так как устанавливать через docker. В целом можно сразу собрать свой образ готовый.
Так же легко будет передать секреты из кубера.

- запуск по расписанию переобучения.

## Kafka

Так как это managed service, то можно не мучиться и поднять вне кубера. Только креды передать в секреты кубера и все.
Поднять желательно через terraform + ansible

- через него будет взаимодействие с сервисом (юзер дергает api, сервер записиывает задачу в топик,
воркеры читают и пишут ответ)

## Kubernetes

кластер содержит в себе воркеры и api.

- Воркеры: один деплоймент, добавить HPA
  - api на fastapi
  - воркеры
- Необходимо заранее подготовить все docker-образы!

## Prometheus

В kubernetes разворачивать или отдельно? Вроде как ставился через докеры, так что удобно в кубере.

- собирает метрики с воркеров? с api точно можно собирать tps и прочее. ПОдумать, какие метрики собирать

## Grafana (Alerting) - под вопросом, стоит ли настроить

В kubernetes разворачивать или отдельно? Вроде как ставился через докеры, так что удобно в кубере.
